name: exam_pulse
description: EXAM PULSE Flutter frontend, demo mode and Node.js API integration.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  file_picker: ^10.1.0
  record: ^6.0.0
  path_provider: ^2.1.5
  pdfx: 2.9.2
  shared_preferences: ^2.3.2
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
